# Shellow web

The public landing page for Shellow. It uses React, Vite, Motion, selected Magic UI patterns, and sanitized screenshots captured from an isolated iOS Simulator build.

```sh
npm install
npm run dev
npm run build
```

Marketing screenshots must only contain fixture hosts, paths, users, and task content. Never capture a personal simulator install or real remote session for this site.
